# Database (replace with your local credentials)
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@localhost:5432/peblonotes

# Auth (replace with a long random string)
JWT_SECRET=replace_with_a_long_random_string

# AI (optional; keep empty if not using paid API)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000